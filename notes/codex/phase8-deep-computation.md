# Phase 8 deep-computation record

Date: 2026-07-31

This record separates factor discovery from the cheap replay certificates used
by the repository tests.  None of the finite computations below is evidence
for an asymptotic density claim.

## Quadratic level 7

For the quadratic transfer with `(d,a0,b0)=(2,1,8)`, direct integer
recurrence gives

```text
E_7 = 78999936752708889867904676370303602782482375124030754597220957868119418164413649973647970771585795292694673449586011206657
```

This is a 122-digit integer.  The Phase 6 bounded search had extracted

```text
189439 * 750692351 * 9825841153
```

and left the 98-digit composite

```text
56536070697207337577943170864114830135531868900762790068048123936021449062339829805776542242181121.
```

### Discovery chain

The executable identified itself as `GMP-ECM 7.0.7 [configured with GMP
6.3.0]`.  The following methods and first-stage bounds were applied to the
successive residuals (ECM curve parameters were generated by the program):

1. Pollard p-1 with `B1=1000000` found `298196593663`, leaving
   `189593281407836178747178993942211664208536197227790900032257434995693494258651852243967`.
2. ECM with `B1=50000` found `991245449894911` on the fourth curve,
   leaving
   `191267744460200361822348833748251233635731867540311359068736421850781697`.
3. Pollard p-1 and p+1 with `B1=1000000` found no factor of that residual.
4. ECM with `B1=50000` found `6726631000961507661177857` on the
   fifty-seventh curve, leaving
   `28434404151626641091139435909034910237447173121`.

The command forms were `ecm -pm1 1000000`, `ecm -pp1 1000000`, and
`ecm -c 100 50000`, with the current residual supplied on standard input.
Because default ECM curves are randomized, the durable result is the exact
division certificate below, not the discovery curve number.

### Replay certificate

The complete factorization is

```text
E_7 =
  189439
  * 750692351
  * 9825841153
  * 298196593663
  * 991245449894911
  * 6726631000961507661177857
  * 28434404151626641091139435909034910237447173121.
```

`notes/codex/fixed_orbit_probe.py` recomputes `E_7`, checks every displayed
factor with SymPy 1.14.0 `isprime`, multiplies the factors exactly, and checks
that each exponent is one.  It also verifies the forced congruence

```text
p mod 512 = 511, 511, 1, 511, 511, 1, 1.
```

The focused regression is

```text
python3 -m unittest notes/codex/test_fixed_orbit_probe.py
```

An independent Claude implementation is responsible for the second
full-product and primality check before this result is described as
dual-verified in the companion note.

## Lucas-Wieferich census through 10^7

The independent Codex implementation uses binary exponentiation in
`Z[X]/(X^2-PX+Q)` and one shared Eratosthenes sieve.  It is algorithmically
separate from Claude's companion-matrix implementation.  The exact command
was

```text
/usr/bin/time -p python3 notes/codex/polynomial_window_check.py --limit 10000000
```

It scanned 664,578 odd primes in 48.02 seconds wall time and returned:

| pair | Lucas-Wieferich primes | ranks | super-Wieferich primes |
|---|---:|---:|---:|
| quadratic `(14,81)` | `65519` | `455` | none |
| cubic `(-2,25)` | `47` | `24` | none |
| quintic `(-6,49)` | `53` | `26` | none |

Thus no new hit occurs between `10^6` and `10^7`.  Claude's independently
implemented census returned exactly the same three sets and no
super-Wieferich prime at the same bound.

The machine-readable output is `notes/codex/census-1e7-record.json`; the
focused regression is

```text
python3 -m unittest notes/codex/test_polynomial_window_check.py
```
