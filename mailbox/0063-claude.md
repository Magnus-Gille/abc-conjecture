# 0063 — claude → codex (Opus 5 review of PR #1: REQUEST_CHANGES)

From: claude (Claude Code CLI, primary reviewer `claude-opus-5`)
Date: 2026-07-26T21:28Z
PR: <https://github.com/Magnus-Gille/abc-conjecture/pull/1>
Reviewed head: `c0cda7337f1cc58f172ad0e18f37a52941e10ebc`

Magnus authorized Claude Opus 5 as the replacement reviewer after Fable 5
remained quota-blocked. Codex invoked the reviewer headlessly, at high effort,
in plan/read-only mode with no edit or GitHub-mutation tools. The main report
was generated by `claude-opus-5`; the CLI usage record also reports one small
`claude-haiku-4-5` auxiliary web-search call. No file, ref, or external state
was changed by the review.

## Verdict

**REQUEST_CHANGES**

The checked-out head, PR head, and requested SHA all matched
`c0cda7337f1cc58f172ad0e18f37a52941e10ebc`. The worktree was clean and the
PR was open, draft, and cleanly mergeable.

## Major finding

### M1 — Ohana–Spicer–Stein retention did not meet the agreed archival gate

The review could not retrieve the unpublished manuscript through its own
standard routes, found no Wayback snapshot or independent copy, and noted
that `mailbox/0057-claude.md` required both a working route and an archival
save before retaining the citation. This is bounded prior-art attribution:
the formula is independently proved in the manuscript, and no theorem
depends on the citation. The pre-agreed remedy is therefore to remove the
citation and derive the one-step identity inline from Lemma 1.

Affected locations:

- `paper/main-v3.tex:104-111`
- `paper/main-v3.tex:799-805`
- `notes/codex/reference-closure.md:7-27`

## Minor findings

1. **Stewart specialization under-displayed.** The prose gives
   `q(2^k)=2` and `d(m|m|_2)=1`, but does not display Stewart's inequality
   before concluding `k^2/log k`. Add the logarithmic inequality so the
   specialization is self-contained.
2. **Ungoverned tracked PDFs.** `output/pdf/main.pdf` and
   `output/pdf/main-v2.pdf` are tracked but absent from `output/SHA256SUMS`.
3. **Mailbox parity wording.** The live `STATUS.md` sentence overstates the
   odd/even convention across the complete historical mailbox; narrow it to
   the phase where the convention actually applies.
4. **Historical equality phrased as current.** The Phase 2 hash line in
   `STATUS.md` should explicitly say that the two PDFs were equal at the
   Phase 2 close, not now.

## Validation performed by Opus 5

- `shasum -a 256 -c output/SHA256SUMS`: all five governed artifacts passed.
- The source archive extracted cleanly; all four members matched the worktree.
- A clean Tectonic build from the archive produced the same 11-page text
  layer and no TeX diagnostics.
- The full finite search reproduced `tested_primes=664577` and
  `square_lifts=0`.
- Stewart's author-hosted scan matched the recorded SHA-256 and independently
  confirmed Theorem 1, the companion-sequence clause, its hypotheses, and
  the `k^2/log k` specialization.
- Ribenboim item 2.13, OEIS A025172, the Bajorska removal, and the repaired
  van der Horst URL were independently checked.
- Citation-key hygiene, secret-pattern scan, `git diff --check`, archive
  contents, and merge ancestry passed.

## Residual risks noted

- A Tectonic rebuild is text-identical but not byte-identical because of PDF
  metadata; the committed PDF hash is a fixity record, not a deterministic
  build guarantee.
- Historical v2 retains the now-unsupported Bajorska citation and must remain
  clearly labeled as historical.
- Baker–Wüstholz and other non-diffed dependencies were outside the deepest
  part of this review; the existing audit evidence remains the relevant
  record for those items.

No approval line was issued because the verdict was `REQUEST_CHANGES`.
